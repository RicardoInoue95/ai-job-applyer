# Versões travadas com == nas que estão em uso e verificadas.
#
# Antes tudo era `>=`, sem pin: uma quebra de API do Playwright ou do Streamlit
# entrava numa reinstalação sem aviso. Para subir uma dependência, altere aqui,
# rode a suíte inteira e commite as duas coisas juntas.
#
# Não é lockfile — as transitivas seguem livres. Se isso virar problema, o passo
# é `pip-compile` gerando requirements.lock.

# ── Web / UI ─────────────────────────────────────────────────────────────────
streamlit==1.58.0          # UI multi-page (descartável por design, ver CLAUDE.md)

# ── Banco ────────────────────────────────────────────────────────────────────
sqlalchemy==2.0.51         # ORM, estilo 2.0 (Mapped/mapped_column)
alembic==1.18.4            # migrations versionadas
psycopg2-binary==2.9.12    # driver PostgreSQL

# ── Automação de browser ─────────────────────────────────────────────────────
playwright==1.60.0         # candidatura em Greenhouse/LinkedIn/Gupy (API sync)

# ── Agendamento ──────────────────────────────────────────────────────────────
apscheduler[sqlalchemy]==3.11.2   # jobs periódicos com jobstore no Postgres

# ── Provedores de LLM ────────────────────────────────────────────────────────
# Instale ao menos um. jobapplier/llm importa cada SDK de forma tardia, então o
# projeto roda com apenas o provedor que você usa.
google-genai==2.9.0        # Gemini — NÃO confundir com o legado google-generativeai
openai==3.1.0              # OpenAI / ChatGPT
anthropic==0.122.0         # Claude

# ── Currículo: parsing e geração ─────────────────────────────────────────────
pydantic==2.13.4           # validação de schema do ResumeJSON
pdfplumber==0.11.10        # extração de texto de PDF
python-docx==1.2.0         # extração de texto de DOCX
reportlab==4.5.1           # geração do PDF do currículo. Era dependência direta
                           # do código e vinha só transitivamente por xhtml2pdf,
                           # que o projeto não usa em lugar nenhum.
pypdfium2==5.10.1          # PDF → imagem, para a regra de inspeção de layout.
                           # Escolhido em vez de PyMuPDF: Apache-2.0/BSD-3 contra
                           # AGPL, o que importa se o projeto virar produto.

# ── Utilitários ──────────────────────────────────────────────────────────────
requests==2.34.2           # APIs de Greenhouse/Lever/Gupy
structlog==26.1.0          # logging estruturado (ver jobapplier/log.py)
click==8.4.1

# ── Desenvolvimento ──────────────────────────────────────────────────────────
pytest==9.1.1
ruff==0.16.3               # lint. O formatter NÃO é aplicado — ver CLAUDE.md
